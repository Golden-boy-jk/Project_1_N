# --- Base ---
DEBUG=True
SECRET_KEY=change-me
ALLOWED_HOSTS=127.0.0.1,localhost

# --- Database ---
# SQLite (по умолчанию, без DATABASE_URL тоже работает)
# DATABASE_URL=sqlite:///db.sqlite3

# PostgreSQL пример:
# DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/newsportal

# --- Cache / Celery ---
REDIS_URL=redis://localhost:6379/0

# --- Email ---
# Консольный бэкенд (для локальной разработки)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP пример (раскомментируй и замени значениями для продакшена):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=myuser
# EMAIL_HOST_PASSWORD=mypassword

# --- Locale ---
TIME_ZONE=Europe/Moscow
LANGUAGE_CODE=ru
